import { memo } from 'react';
// import { authUser } from '../store';
// import { useObservableData } from '../utils/useObservable';

export const Home = memo(() => {
  // const isLogging = useObservableData(authUser.isLogging$);
  
  // console.log('Home', isLogging);
  // console.log('Home');

  return (
    <div>
      Home
    </div>
  );
});
